import SwiftUI

struct HistoryView: View {
    
    var body: some View {
        
        NavigationStack{
            Text("Profile")
                .font(.largeTitle)
                .fontWeight(.bold)
            Text("You have spent () out ()")
            Text("You can spend () more and still stay in budget")
            
            
            
            
            }
        }
    }
