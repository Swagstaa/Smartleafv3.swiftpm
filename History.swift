import SwiftUI

struct HistoryView: View {
    var body: some View {
        VStack {
            Text("Budget spent on")
                .frame(width: 100, height: 100, alignment: .trailing)
            
           }
        }
    }
